# mdtopdf — Claude Code 规范

## 1. Conventional Commits（强制）

所有 commit 必须遵循 [Conventional Commits v1.0.0](https://www.conventionalcommits.org/) 规范。

### 格式

```
<type>(<scope>): <subject>

[optional body]

[optional footer]
```

### 允许的 type

| type       | 用途                         |
|------------|------------------------------|
| `feat`     | 新功能                       |
| `fix`      | Bug 修复                     |
| `docs`     | 文档变更                     |
| `style`    | 格式（不影响逻辑）           |
| `refactor` | 重构（非 feat/fix）          |
| `perf`     | 性能优化                     |
| `test`     | 添加/修改测试                |
| `build`    | 构建系统或外部依赖变更       |
| `ci`       | CI/CD 配置变更               |
| `chore`    | 其他杂项（不修改 src/test）  |
| `revert`   | 回滚                         |

### 规则

- `subject` 不得首字母大写，不得以句点结尾
- `header` 总长度不超过 100 字符
- 破坏性变更须在 footer 添加 `BREAKING CHANGE:` 说明

### 示例

```
feat(cli): add --watch flag for live conversion
fix(convert): handle empty markdown files gracefully
docs: update README with installation steps
ci: add commitlint to pull request workflow
```

---

## 2. 转换操作自动记录 Log

每次调用 `convertMdToPdf()` 后，系统自动向 `logs/conversions.log` 追加一条记录。

### 日志格式

```
[ISO 8601 timestamp] SUCCESS input="<path>" output="<path>"
[ISO 8601 timestamp] FAILURE input="<path>" error="<message>"
```

### 规则

- `logs/` 目录已加入 `.gitignore`，不提交到版本库
- 日志只追加，不覆盖
- 每次新增或修改转换逻辑后，须确保日志调用仍然覆盖所有出口路径（成功与失败）
- 若添加批量转换功能，每个文件单独记录一行

---

## 3. 分支与 PR 规范

- 主分支：`main`（受保护，禁止直接 push）
- 功能分支命名：`feat/<short-description>`
- 修复分支命名：`fix/<short-description>`
- PR 标题必须符合 Conventional Commits 格式
- PR 合并前须通过 CI（commitlint + build check）
